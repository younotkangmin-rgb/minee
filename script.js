const minerals = [\n    { name: 'Diamond', rarity: 'Rare' },\n    { name: 'Gold', rarity: 'Uncommon' },\n    { name: 'Iron', rarity: 'Common' },\n    { name: 'Coal', rarity: 'Common' },\n    { name: 'Emerald', rarity: 'Rare' },\n];\n\nlet inventory = [];\n\nfunction getRandomMineral() {\n    const randomIndex = Math.floor(Math.random() * minerals.length);\n    return minerals[randomIndex];\n}\n\nfunction updateInventoryDisplay() {\n    const inventoryElement = document.getElementById('inventory');\n    inventoryElement.innerHTML = '';\n    inventory.forEach(mineral => {\n        const mineralElement = document.createElement('div');\n        mineralElement.textContent = mineral.name;\n        inventoryElement.appendChild(mineralElement);\n    });\n}\n\nfunction updateCollectionDisplay() {\n    // Assume collection is a similar structure to inventory\n}\n\nfunction updateSellDisplay() {\n    const sellElement = document.getElementById('sell');\n    sellElement.innerHTML = '';\n    inventory.forEach(mineral => {\n        const sellButton = document.createElement('button');\n        sellButton.textContent = 'Sell ' + mineral.name;\n        sellButton.onclick = () => sellMineral(mineral);\n        sellElement.appendChild(sellButton);\n    });\n}\n\nfunction sellMineral(mineral) {\n    const index = inventory.indexOf(mineral);\n    if (index > -1) {\n        inventory.splice(index, 1);\n        updateInventoryDisplay();\n        updateSellDisplay();\n    }\n}\n\nfunction updateCraftDisplay() {\n    // similar to other display functions\n}\n\nfunction updateStatsDisplay() {\n    const statsElement = document.getElementById('stats');\n    statsElement.innerHTML = '';\n    // Display stats here\n}\n\ndocument.getElementById('pull-button').addEventListener('click', () => {\n    const mineral = getRandomMineral();\n    inventory.push(mineral);\n    updateInventoryDisplay();\n    updateSellDisplay();\n});